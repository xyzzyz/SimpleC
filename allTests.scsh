#!/usr/bin/scsh \
-e main -s
!#
(define (run-test name)
  (display (format #f "~a~%" name))
  (run (../TestParser) (< ,name))
  (display (format #f "~%")))

(define (main arg)
  (with-cwd "tests/"
            (for-each run-test (directory-files))))

